meta:
  owner: me2
  ownerid: me2
  dataset: id2
  version: 1
  findability: published
inputs:
- kind: datapackage
  parameters:
    resource-mapping:
      vix-daily: data/dummy.csv
    descriptor:
      name: single-file
      resources:
        -
          name: birthdays
          path: data/birthdays.csv
          format: csv
          schema:
            fields:
              -
                name: date
                type: date
  url: datapackage.json
